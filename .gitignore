# Go
/incipit
/seed
*.test
*.out

# Node / frontend
/frontend/node_modules/
/frontend/dist/
*.tsbuildinfo

# Built frontend assets (generated by the Vite build / Docker). The placeholder
# web/dist/index.html is tracked so `go build` works without a frontend build.
/web/dist/assets/

# Local runtime state
/config/
/library/
.DS_Store
